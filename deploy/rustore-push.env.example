# RuStore Push — сервисные токены (не коммитить)
# Консоль → Инструменты → Push → Проекты → ваш проект

# ID проекта (из консоли Push → Проекты)
RUSTORE_PUSH_PROJECT_ID=Ya1db13x4k9ch76Hc-zOf6fhDY6B7KYA

# Активный токен (используется только один)
RUSTORE_PUSH_SERVICE_TOKEN=

# Резервный (хранить отдельно, не использовать одновременно)
RUSTORE_PUSH_SERVICE_TOKEN_BACKUP=
