# Скопируйте: cp deploy/notify.env.example deploy/notify.env
# Затем: npm run vps:notify

# --- Email (REG.RU почта) ---
SMTP_HOST=mail.hosting.reg.ru
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=noreply@nagaevomaster.ru
SMTP_PASS=
SMTP_FROM=Нагаево Мастер <noreply@nagaevomaster.ru>

# Письмо при новом объявлении на модерации
MODERATOR_NOTIFY_EMAIL=Grom666e@gmail.com
SITE_URL=https://nagaevomaster.ru

# --- SMS (https://sms.ru → API) ---
SMS_RU_API_ID=
