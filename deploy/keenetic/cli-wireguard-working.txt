# Политики и применение — только вручную (скрипты их не трогают).
# Туннель WireGuard: npm run keenetic:wg-import && npm run keenetic:wg-policy

# --- Туннель (скрипт keenetic:wg-policy делает это сам) ---
# interface Wireguard0 security-level private
# ip nat Wireguard0
# no isolate-private

# --- Политики (пример, настраивайте сами) ---
# interface ISP ip global order 0
# interface Wireguard0 ip global order 1
# ip policy Policy2
# permit global Wireguard0 order 0
# exit
# ip hotspot host aa:bb:cc:dd:ee:ff policy Policy2

# Проверка:
# show interface Wireguard0 → connected: yes
# show ip policy
